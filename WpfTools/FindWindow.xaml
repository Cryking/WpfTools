<Window x:Class="WpfTools.FindWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTools"
        mc:Ignorable="d"
        Title="查找" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 查找输入区域 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <Label Content="查找内容:" FontWeight="Medium" Margin="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Name="txtSearchText" 
                         Grid.Column="0"
                         Height="25"
                         Padding="5"
                         VerticalContentAlignment="Center"
                         BorderBrush="#D1D5DB"
                         BorderThickness="1"
                         Background="White"/>
                
                <CheckBox Name="chkCaseSensitive" 
                          Grid.Column="1"
                          Content="区分大小写" 
                          VerticalAlignment="Center"
                          Margin="10,0,0,0"/>
            </Grid>
        </StackPanel>
        
        <!-- 按钮区域 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Name="btnFindNext" 
                    Content="查找下一个" 
                    Width="80" 
                    Height="30"
                    Margin="5"
                    Background="#4A90E2"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="BtnFindNext_Click"/>
            
            <Button Name="btnFindPrevious" 
                    Content="查找上一个" 
                    Width="80" 
                    Height="30"
                    Margin="5"
                    Background="#4A90E2"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="BtnFindPrevious_Click"/>
        </StackPanel>
        
        <!-- 状态信息 -->
        <Border Grid.Row="2"
                HorizontalAlignment="Center"
                Margin="0,15,0,0"
                Background="#F8F9FA"
                CornerRadius="4"
                Padding="10,5">
            <TextBlock Name="txtStatus" 
                       FontSize="13"
                       FontWeight="Medium"
                       Foreground="#2C3E50"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>