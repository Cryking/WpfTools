<Window x:Class="WpfTools.ReminderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTools"
        mc:Ignorable="d"
        Title="定时提醒设置" Height="280" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#357ABD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2968A8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="定时提醒设置" FontSize="18" FontWeight="Bold" 
                   Margin="0,0,0,15" Foreground="#2C3E50" HorizontalAlignment="Center"/>
        
        <TextBlock Grid.Row="1" Text="提醒间隔时间（分钟）：" Margin="0,5" 
                   FontSize="14" Foreground="#2C3E50"/>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
            <TextBox Name="txtMinutes" Width="100" Height="30" FontSize="14" 
                     VerticalContentAlignment="Center" Text="30"/>
            <TextBlock Text="分钟" Margin="5,0,0,0" VerticalAlignment="Center" 
                       FontSize="14" Foreground="#2C3E50"/>
        </StackPanel>
        
        <TextBlock Grid.Row="3" Text="提醒内容：" Margin="0,10" 
                   FontSize="14" Foreground="#2C3E50"/>
        
        <TextBox Name="txtMessage" Grid.Row="4" Height="80" FontSize="14" 
                 TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 Text="该休息一下了！记得喝水、活动一下身体。"/>
        
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Name="btnStart" Content="开始提醒" Width="100" Height="35" 
                    Style="{StaticResource ButtonStyle}" Click="BtnStart_Click"/>
            <Button Name="btnStop" Content="停止提醒" Width="100" Height="35" 
                    Style="{StaticResource ButtonStyle}" Click="BtnStop_Click"/>
        </StackPanel>
    </Grid>
</Window>